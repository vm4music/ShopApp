<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!--========== BOX ICONS ==========-->
    <link href='https://cdn.jsdelivr.net/npm/boxicons@2.0.5/css/boxicons.min.css' rel='stylesheet'>

    <!--========== CSS ==========-->
    <link rel="stylesheet" href="/assets/css/styles.css">

    <title>Little Bugs</title>
</head>

<body>

    <!--========== SCROLL TOP ==========-->
    <a href="#" class="scrolltop" id="scroll-top">
        <i class='bx bx-chevron-up scrolltop__icon'></i>
    </a>

    <!--========== HEADER ==========-->
    <%- include('header') -%>

        <main class="l-main">
            <!--========== HOME ==========-->
            <!-- <section class="home" id="home">
                <div class="home__container bd-container bd-grid">
                    <div class="home__data">
                        <h1 class="home__title">Tasty food</h1>
                        <h2 class="home__subtitle">Try the best food of <br> the week.</h2>
                        <a href="/product" class="button">View Menu</a>
                    </div>
    
                    <img src="/assets/img/home.png" alt="" class="home__img">
                </div>
            </section> -->

            <!--====================FILTERS======================-->


            <!--========== PRODUCT LIST ==========-->
            <section class="menu section bd-container" id="menu">
                <span class="section-subtitle">Special Sale</span>
                <!-- <h2 class="section-title">New Collection </h2> -->
                <!-- 
                    <h2 class="section-title">
                    <%= data.qry %>
                </h2> -->
               
                    <div class="filter">

                        <div class="container_results">
                            <div class="dropdown">
                                <button class="dropbtn">Sort</button>
                                <div class="dropdown-content">
                                    <% if(data.previous || data.next) { %>
                                        <a href="/shop?sort=Price Low-to-High&text=<%= data.text %>">Price Low-to-High</a>
                                        <a href="/shop?sort=Price High-to-Low&text=<%= data.text %>">Price High-to-Low</a>
                                        <% } %>
                                </div>
                            </div>

                            <% if(data.product_categories){%>
                            <% if(data.qry !="" ) {%>
                                <h3 class="section-subtitle">Search results for: "<%= data.qry %>"</h3>
                                <%} if(data.result.length==0){%>
                                    <h2 class="section-subtitle">No Results found for your query</h2>
                                    <%}%>
                        </div>
                        <div class="sort__filter__box">
                            <% if(data.qry){ %>
                                <div class="sort__filter__box__a">
                                    <p>
                                        <a href="/">Home</a> / <a href="/searchcategory/<%= data.qry %>">
                                            <b>
                                                <%= data.qry %>
                                            </b>
                                        </a>
                                    </p>
                                </div>
                                <%}%>
                                    
                                    <form action="/searchcategory" method="get" class="search__filter">

                                        <select class="filter__box" id="categorysearch" name="categorysearch"
                                            onchange="this.form.submit()">
                                            <% data.product_categories.forEach(category=> { %>
                                                <% if(data.category_selected==category){%>
                                                    <option value="<%= category %>" selected>
                                                        <%= category %>
                                                    </option>
                                                    <%}else{%>
                                                        <option value="<%= category %>">
                                                            <%= category %>
                                                        </option>
                                                        <%}%>
                                                            <%});%>
                                        </select>


                                        <select class="filter__box" id="categorysearch" name="sortsearch"
                                            onchange="this.form.submit()">
                                            <!-- <option value="">--none--</option> -->
                                            <% data.sort_categories.forEach(category=> { %>
                                                <% if(data.sort_selected==category){%>
                                                    <option value="<%= category %>" selected>
                                                        <%= category %>
                                                    </option>
                                                    <%}else{%>
                                                        <option value="<%= category %>">
                                                            <%= category %>
                                                        </option>
                                                        <%}%>
                                                            <%});%>
                                        </select>
                                    </form>
                        </div>
                        <%}%>
                    </div>
                   
                        <div class="menu__container bd-grid">
                            <% for(i in data.results) { %>
                                <div class="menu__content"> <img src="/assets/img/<%= data.results[i].img %>" alt=""
                                        class="menu__img">
                                    <h3 class="menu__name">
                                        <%= data.results[i].name %>
                                    </h3>
                                    <!-- <span class="menu__detail"><%= data.results[i].detail %></span> -->

                                    <span class="menu__preci">INR <%= data.results[i].price %></span>
                                    <a href="/product/<%= data.results[i].p_id %>" class="button menu__button"><i
                                            class='bx bx-cart-alt'></i></a>
                                </div>
                                <!-- <%= data.results[i].p_id %>
                    <%= data.results[i].name %> -->
                                <% }; %>
                            </div>
                                    <!--========== PAGINATION ==========-->
                        
                        <div class="pagination">
                            <% if(data.previous) { %>
                                <a href="/shop?page=<%= data.previous.page %>&sort=<%= data.sort %>&text=<%= data.text %>">&laquo;</a>
                                <% } %>
                            <% if(data.next) { %>
                                <a href="/shop?page=<%= data.next.page %>&sort=<%= data.sort %>&text=<%= data.text %>">&raquo;</a>
                                <% } %>
                            <!-- <a href="/shop?page=1"></a> -->
                           
                            <% if(data.pages) { %>
                                <% for(var i=1;i<=data.pages; i++) {%>
                                    <% if(data.page_selected==i){%>
                                        <a href="/searchcategory/<%= data.qry %>?page=<%= i %>" class="active">
                                            <%= i %>
                                        </a>
                                        <%} else{%>
                                            <a href="/searchcategory/<%= data.qry %>?page=<%= i %>">
                                                <%= i %>
                                            </a>
                                            <%}}%>
                                                <%}%>
                                                   
                        </div>
            </section>

            <!--===============SERVICES SECTION================-->
            <%- include('services') -%>

                <!--========== CONTACT US ==========-->
                <section class="contact section bd-container" id="contact">
                    <div class="contact__container bd-grid">
                        <div class="contact__data">
                            <span class="section-subtitle contact__initial">Let's talk</span>
                            <h2 class="section-title contact__initial">Contact us</h2>
                            <p class="contact__description">If you want to reserve a table in our restaurant, contact us
                                and we will attend you quickly, with our 24/7 chat service.</p>
                        </div>

                        <div class="contact__button">
                            <a href="#" class="button">Contact us now</a>
                        </div>
                    </div>
                </section>
        </main>

        <!--========== FOOTER ==========-->
        <%- include('footer') -%>

            <!--========== SCROLL REVEAL ==========-->
            <script src="https://unpkg.com/scrollreveal"></script>

            <!--========== MAIN JS ==========-->
            <script src="/assets/js/main.js"></script>
</body>

</html>